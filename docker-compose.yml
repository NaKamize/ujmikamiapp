services:
  db:
    image: mysql:8.0
    environment:
      MYSQL_DATABASE: ujmikamiapp
      MYSQL_USER: ujmikamiapp
      MYSQL_PASSWORD: ujmikamiapp
      MYSQL_ROOT_PASSWORD: rootpassword
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s
    restart: unless-stopped

  backend:
    build:
      context: ./backend/ujmikamiapp
      dockerfile: Dockerfile
    environment:
      - PRODUCTION=false
    env_file:
      - ./backend/ujmikamiapp/.env
    volumes:
      - ./backend/ujmikamiapp:/app
    depends_on:
      db:
        condition: service_healthy
    ports:
      - "8000:8000"
    restart: unless-stopped

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
      args:
        REACT_APP_API_BASE_URL: ""
        REACT_APP_AI_SERVICE_URL: "http://localhost:8002"
    volumes:
      - ./frontend:/app
      - /app/node_modules
    depends_on:
      - backend
    ports:
      - "3000:80"
    restart: unless-stopped

  chroma-db:
    image: chromadb/chroma:latest
    environment:
      - IS_PERSISTENT=TRUE
      - PERSIST_DIRECTORY=/data
    volumes:
      - chroma_data:/data
    ports:
      - "8001:8000"
    restart: unless-stopped

  fastapi-ai:
    build:
      context: ./fastapi-ai
      dockerfile: Dockerfile
    environment:
      - LLM_PROVIDER=ollama
      - CHROMA_MODE=http
      - CHROMA_HOST=chroma-db
      - CHROMA_PORT=8000
      - OLLAMA_BASE_URL=http://host.docker.internal:11434
      - CORS_ALLOWED_ORIGINS=http://localhost:3000
    extra_hosts:
      - "host.docker.internal:host-gateway"
    volumes:
      - ./fastapi-ai:/app
    depends_on:
      - chroma-db
    ports:
      - "8002:8000"
    restart: unless-stopped

volumes:
  mysql_data:
  chroma_data: