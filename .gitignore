**/node_modules/
**/.venv/
**/.env
**/__pycache__/
**/*.sqlite3
.opencode/
.mypy_cache/
*.swp
azure.txt

# Test/coverage artifacts (regenerated by `coverage run` / `npm test -- --coverage`)
.coverage
htmlcov/
frontend/coverage/

# CRA production build output — Azure Static Web Apps builds fresh from source on deploy,
# and every local `npm run build` produces new content-hashed filenames (pure diff churn)
frontend/build/