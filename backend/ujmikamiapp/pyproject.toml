[tool.ruff]
target-version = "py312"
line-length = 120
exclude = ["*/migrations/*", ".venv"]

[tool.ruff.lint]
select = ["E", "F", "W", "I"]

[tool.ruff.lint.per-file-ignores]
"*/management/commands/*.py" = ["E501"]  # long-form seed/copy text, not code complexity
